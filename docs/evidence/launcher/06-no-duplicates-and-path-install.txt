# no duplicate containers after 1 setup + 3 launches/starts:
$ docker ps -a --filter label=com.docker.compose.project=sentinelops-fresh --format "{{.Label \"com.docker.compose.service\"}}" | sort | uniq -c
      1 api
      1 demo-app
      1 dispatcher
      1 executor
      1 migrate
      1 monitor
      1 notifier
      1 notify-sink
      1 ops-reader
      1 postgres
      1 redis
      1 worker

# optional: bare command on PATH
$ which sentinelops
(not installed yet: the bare command does not exist)
$ uv tool install --editable .
 + watchfiles==1.3.0
 + websockets==17.1
Installed 1 executable: sentinelops
$ cd /tmp && sentinelops status
Remediation: human approval required (isolated demo only)
Agent overall: degraded
  executor: degraded

Dashboard: http://127.0.0.1:8000/dashboard/   (local only; bound to 127.0.0.1)
  It asks for the read-only API token: the SENTINEL_API_READ_TOKEN value in /tmp/claude-1000/-workspaces-Groot-002/91611681-be2f-4c80-ab1d-936e4de5fedf/scratchpad/fresh/sentinelops/.env (not printed here).
[exit code 0]
$ uv tool uninstall sentinelops
Uninstalled 1 executable: sentinelops
(removed)
